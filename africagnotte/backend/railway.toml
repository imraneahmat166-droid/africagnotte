# railway.toml — Configuration Railway pour AfriCagnotte Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"

[[services]]
name = "africagnotte-api"
